NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional LAN-only MediaMTX host. Broadcast controls are unavailable when omitted.
# Use only a hostname or IPv4 address, without a scheme, port, path, or credentials.
NEXT_PUBLIC_MEDIAMTX_HOST=192.168.0.106
